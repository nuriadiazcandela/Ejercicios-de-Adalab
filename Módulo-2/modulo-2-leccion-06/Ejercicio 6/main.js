"use strict";

const pears = {
    max: 15,
    min: 1,
    actual: 10,
    inicial: 8,
}

 pears.add = (pears.actual + 1);
 console.log (pears.add)

pears.remove = (pears.actual -2);
console.log (pears.remove);




