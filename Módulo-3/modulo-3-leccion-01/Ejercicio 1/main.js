'use strict';

class Square {
  perimeter(result) {
    return result * 4;
  }
  area() {
    console.log(math.pow(9, 2));
  }
}

const side = new Square();
side.perimeter();
side.area();
