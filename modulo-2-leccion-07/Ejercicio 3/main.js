"use strict";
let acc = 0;
for (acc = 0; acc < 11; acc = acc + 2) {
  console.log(`voy por la vuelta ` + acc);
}
//me falta el final del ejercicio Mostrar el resultado es : X, siendo X el valor de la variable acc
